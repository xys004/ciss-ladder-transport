# Figures

This directory stores figure-ready outputs generated from the referee-facing scripts.

- source data should come from `data/raw/` or `data/processed/`
- manuscript-ready images should be generated explicitly, not edited in place without provenance
