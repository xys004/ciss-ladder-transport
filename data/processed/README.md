# Processed Data

This directory is intended for processed datasets derived from the raw spectral outputs in `data/raw/`.

Typical examples:

- integrated `I_z` scans versus chain length
- figure-ready tables combining multiple raw datasets
