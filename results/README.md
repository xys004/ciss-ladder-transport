# Results Placeholder

Default output directory for processed current-vs-length tables generated by:

- `scripts/current_from_transmission.py`

This directory is ignored for routine generated files, but the folder itself is tracked so the repository layout remains clear.
